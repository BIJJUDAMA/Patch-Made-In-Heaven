# CLAUDE.md — Claude Agent Entry Point

This repository's agent workflow, documentation map, decision log, and handoff protocol are defined once, in `AGENTS.md`, to avoid drift between tool-specific entry points.

If you are Claude (Claude Code, Claude Desktop, or otherwise) resuming work on **HackOverflow**: read `AGENTS.md` in full before making any changes. Everything that would otherwise live in this file — the documentation map, the specification-first workflow, the human approval gate, the HANDOFF.md protocol, and the commit standards — lives there instead.
