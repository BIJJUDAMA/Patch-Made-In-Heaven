# NitroStack & Patch Made In Heaven Configuration Template
NITRO_LOG_LEVEL=info
NITROSTACK_APP_MODE=universal

# Search & Knowledge Base (Elasticsearch)
ELASTICSEARCH_URL=
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_REQUEST_TIMEOUT_MS=30000

# Embeddings & Fast LLM Reasoning (OpenRouter / Groq)
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_EMBEDDING_MODEL=nvidia/nemotron-3-embed-1b:free
# Must match this model's real output size — confirmed 2048 via NVIDIA's own
# model card and a live API call (this codebase never requests a truncated
# size). Never hardcode a different value for a different embedding model.
EMBEDDING_VECTOR_DIMENSIONS=2048
EMBEDDING_REQUEST_TIMEOUT_MS=15000

GROQ_API_KEY=
GROQ_REASONING_MODEL=qwen/qwen3.6-27b

# Search Result Limits
SEARCH_RESULT_LIMIT_DEFAULT=5
SEARCH_RESULT_LIMIT_MAX=25

# Sandbox Verification
SANDBOX_CONTAINER_RUNTIME=docker
SANDBOX_TIMEOUT_MS=10000
SANDBOX_MEMORY_LIMIT=256m
SANDBOX_CPU_LIMIT=1
SANDBOX_PIDS_LIMIT=128
SANDBOX_MAX_OUTPUT_BYTES=65536

# Production Sandbox Provider (Decision 010, Phase 6). Explicit opt-in only —
# always defaults to "local" (Docker/podman). Set to "e2b" only on the actual
# NitroCloud deployment, where no container engine exists at all. Never set
# this to "e2b" on a local dev machine or in CI.
SANDBOX_PROVIDER=local
E2B_API_KEY=

# Web Dashboard MCP Endpoint
NEXT_PUBLIC_MCP_SERVER_URL=https://patch-made-in-works-on-my-machine-amrita-university-coimbatore.app.nitrocloud.ai/mcp

